%h1.heading
  = "User"
  
- content_tag_for(:dl, @user) do
  %dt.label= 'Email'
  %dd.email= h @user.try(:email)
  %dt.label= 'Password'
  %dd.password= h @user.try(:password)
  
%p.actions
  = link_to t(:edit).capitalize, edit_admin_user_path(@user)
  |
  = link_to 'Index', admin_users_path